import 'package:flutter/material.dart';

class TwitterSpaces extends StatelessWidget {
  const TwitterSpaces({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
